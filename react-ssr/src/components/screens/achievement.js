import {config} from "../../configs/config";
import BasicDisplay from "../common/basicDisplay";


const Achievement= props=>{
    return <div><BasicDisplay list={config.localdata.ACHIEVMENT}/></div>
}
export default Achievement
